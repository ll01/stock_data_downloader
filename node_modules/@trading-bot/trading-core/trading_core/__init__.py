"""
Trading Core - Consolidated library for trading applications.

This package contains:
- Trading domain models and configurations
- Common utilities and shared components
- Configuration models for trading applications

Merged from trading-core and common packages for simplicity.
"""

__version__ = "0.1.0"

# Export the main components
from .models import (
    HestonConfig, GBMConfig, TickerConfig, TickerData,
    CandleData, Trade, PairData, TickerStats,
    DataSourceType, ServerConfig, DataSourceConfig, ExchangeConfig,
    StrategyConfig, MonitoringConfig, AppConfig
)

from .metrics_publisher import MetricsPublisher